# 审计遗留问题（2026-09-08）

> 三轮审计（一轮自审 + 两轮独立 agent）里**发现了但没有动**的问题。与另几份文档的分工：
> [DESIGN.md §10](DESIGN.md) 只留指针，本文记全部未完成项——待核的数字、
> 单方面定下需要确认的决策、已知未修的缺口。**修掉一条删一条，本文为空即可删除。**
>
> 每条写四样：在哪、现状、为什么没动、要动需要什么。
>
> **K6、G6–G11 不来自那三轮**：K6 / G6 是 2026-09-09 扫三方项目 teamai-cli、并对那两份文档做
> 对抗性审计时反照出来的（见 [third-party/](third-party/teamai-cli-vs-vibetrail.md)）；
> G7 / G8 / G9 是同日用户看完对比后直接提的需求。G10 是 2026-09-10 扫 LoongSuite Pilot、把对比扩为三方时反照出来的。
> G11 是 2026-09-10 用户看完三方对比后提的需求。**G7 已于 2026-09-14 定稿进 [DESIGN.md](DESIGN.md)**，它的未定项拆成本表 U1–U10，
> 随旧方案（仓内投影、`Claude-Session` trailer）退役的条目同日关闭；2026-09-15 D5（不传 transcript 原文件、云端定协议 1.0、30 天够用）关 U3 / U10 / K1，G5 升前置；同日做完第 1 步「提取器扩展与协议映射」，关 G5、立 U11，随后定 U11、立 U12；G11 的方案细节另记在 [TODO.md](TODO.md)，本表只记一句话与状态。**2026-09-16 复核 G7 已完成的部分**（本机 124,666 条真实事件逐项核 + teamai / Pilot 源码各审一遍）：K8 升 🔴 并找到修法、K7 的待实测关闭、立 K12–K16 与 U16；push 方案的修正记在 TODO。同日用户定运行时全部换成 Node 单文件 `.mjs`、去掉 jq（DESIGN D12），移植拆解在 TODO，移植期间 jq 版冻结只修 🔴。
> 又一次印证 §D 第一条：**盲区要靠换个视角才照得出来**——这次的「视角」是拿别人的
> 实现当镜子，成本比再起一轮审计低得多。

## A–B. 已解决的两批（2026-09-09），只留一句话

**A 四组待核数字**：重数后全部不是统计错误（两种口径、表格结构、口径未写明、测量时点不同）。
**B 五条单方面决策**：B1 审计锚（后又改为 `Vibetrail-Id` trailer，见 [spec/trace-v1.md §2](spec/trace-v1.md)）、B2 编辑器路径不注入 trailer、
B4 `.gitattributes` 的 `merge=union`、B5 删 `.gitignore:9` 引用——B2 随 `Claude-Session` trailer 退役（2026-09-14，D4），B4 只剩审计线在用；
B3 删掉 `user_edited_after_agent` 进了 [spec/diverge-v1.md §2.1](spec/diverge-v1.md)。两批的教训并入 §D。



## C. 中心表：所有未完成项

> **这是唯一的未完成项清单。** DESIGN.md §10 与 CAPABILITIES.md §3 只留指针，不重复描述——
> 三处各记一份是上一版的实际状态，其中至少三组是同一件事（hook 无引导 / 提取器去重 /
> `core.hooksPath`），正是「必须与 X 保持一致」那类会漂的拷贝。

| ID | 类型 | 优先级 | 一句话 |
|---|---|---|---|
| ~~**G1**~~ | 功能缺口 | ✅ | ~~查询端完全缺失~~ —— **已实现** `old/vibetrail`。做完当场照出一个写入端缺陷（见 K5）|
| ~~**G2**~~ | 功能缺口 | ✅ | ~~session 流水的写入者未定义~~ —— **由 D4 关闭**（2026-09-14）：分歧由 hook 在 UserPromptSubmit / Stop / SessionEnd / SessionStart 补做时写本机 spool，仓内 `sessions/` 停用（DESIGN §3.1） |
| **K6** | 已知缺陷 | 🟡 | **2026-09-14 用户定暂缓：「暂时先不考虑脱敏」，不挡 G7 的上传，G7 落地后再定。D4 之后暴露面从「随代码入仓」变成「全量副本原样上云」；2026-09-15 D5 后再缩成「分歧事件带的几段正文原样上云」（被拒命令、被打断回复、打断后的人话，DESIGN §4 隐私行），但云端默认全公司可见，已提意见；**09-16 用户定全采正文、不脱敏**（DEMO §5；DESIGN 还没记，K25），暴露面又变成全部正文：人的 prompt、模型输出、thinking、工具参数与结果、system prompt，D13 之后还有 CLAUDE.md 与 auto memory 的正文；下面原记的 `sessions/` 两处已随投影退役，`audits/` 两处仍在。** 原记：**trace 已落自由文本，且一处脱敏都没有**——`audits/*.jsonl` 的 `findings[].claim`（审计结论正文，**由审计 agent 生成**，正文里完全可能带上被审代码片段、路径、密钥样本）与 `agents[].perspective`；`sessions/*.jsonl` 的 `end.subagents[].desc` 与 `session.cwd`（绝对路径，带用户名与项目名）。这些**随代码入仓**，推远端即团队可见。三方对照：teamai-cli 的对等字段（`promptSummary` / `firstPrompt`）**全部强制过 `redactWithEnv()`**，且团队推送默认只推计数与工具名、自由文本要显式 opt-in。要动：把「落自由文本前必须过脱敏」写进 [spec/diverge-v1.md §6](spec/diverge-v1.md) 与 [spec/trace-v1.md §4](spec/trace-v1.md) 的稳定面，并给 `claim` 补脱敏环节；全量副本的出本机那层按 DESIGN §4 隐私行的三层准入做。**原 K4（分支名）是本条的子集，已并入**。**LoongSuite Pilot 提供了更完整的样板与三条教训**（[采集清单 §5 / §6](third-party/loongsuite-pilot-collection.md)）：模型上它把「采不采」（`captureMessageContent`）与「擦不擦」（`mask`）拆成管线里两个独立步骤，比单一开关表达力强；字段准入实际是**三层**——白名单（`DEFAULT_RESOURCE_ENV_FIELD_MAP` 只放行 2 个 env）+ **按 key 名拒绝整个字段**（`SENSITIVE_FIELD_NAME_RE = /(^\|[_.-])(TOKEN\|SECRET\|PASSWORD\|CREDENTIAL\|COOKIE)([_.-]\|$)\|^(API_KEY\|API_HEADER)$/i`，用分隔符做词边界所以 `TOKENIZER` 不误伤，`assets/hooks/shared/resource-context.mjs:5`）+ 值长度上限 512。**「按 key 名拒绝」这一层我们完全没有**，而它比按值匹配更可靠。三条教训都是我们可能犯的：① **出口那层要有测试守着**——它的出站兜底 `redactCodeGenerationFields()` 删的是大部分内容字段（清单里漏了 system prompt、工具定义和多模态元数据，也没有 `error.message`），又因 `endpoint.redact` 全仓硬编码 `false` 而永不执行，写了、合理、就是不生效；② **字段清单要有单一真相源**——它的内容策略有两份 `MESSAGE_CONTENT_FIELDS`（hook 侧 14 项 / daemon 侧 17 项），已经分叉，且**两份都漏了 `error.message`**（工具失败时的结果正文前 500 字符），于是关掉内容采集它照样出本机；③ **默认值决定实际效果**——两层设计得再好，默认 `true` + `none` 就是「全采不脱」。另有一条直接对上我们 `session.cwd` 的硬闸：`docs/agent-onboarding.md:368` 规定 fixture **不得含**真实 prompt、transcript、用户名、home 路径、仓库路径、session ID、凭据 |
| ~~**G3**~~ | 功能缺口 | ✅ | ~~没有「保证每人跑过 install」的机制~~ —— **前提消失**（2026-09-14，D4）：G7 不装 git hook、被观测仓零写入、机器级装一次，没有东西要每人装。审计线自己的 git hook 是否保留见 U6 |
| **K5** | ~~已知缺陷~~ | ✅ | ~~一次「拒绝工具调用」被记两条~~ —— **已修**：`for tool use` 变体拆成独立 kind。实测 35 = 35 精确对上（见 [spec/diverge-v1.md §2](spec/diverge-v1.md)）|
| ~~**K1**~~ | 已知缺陷 | ✅ | ~~提取器重复计数未去重~~ —— **由 D5 关闭**（2026-09-15）：父会话文件里的打断映射成 `turn.end(interrupted)`、子 agent 文件里的映射成 `subagent.end(cancelled)`，协议里是两个事实，不合并；统计打断只数 `turn.end`。下面的方案与 Pilot 的教训留给将来要合并的人。原记：方案已定（同 sid + 同 kind + ≤10 秒，靠 `isSidechain`/`agentId` 分层），但 `hit` 还没输出这两个字段。⚠️ **方案里的「≤10 秒」需要复核**：LoongSuite Pilot 在同一个问题上（子 agent 记录并入父 trace）的结论是 `docs/codex-subagent-fusion.md:9-21`——① 去重键必须用**父侧的 tool-call id**，`agent_path`（≈ 我们的 agent 名 / 目录）**明确不得做去重键**，因为它会被多次 spawn 复用；② **`time_order` 只有诊断权**，「可以在关联快照和日志里解释一个可能的关系，但不能创建融合候选、不能捕获子终态、不能延迟父终态」（原文 fusion candidate：把子记录并进父 trace 的候选，对应我们的合并去重）。我们的「≤10 秒」正是这一类信号：结构性键（`agentId` / 记录 uuid）分层是对的，时间窗若**单独**决定合并，两次真实独立的同类分歧（例如用户连续拒绝两个同名工具调用）会被并成一条，而且不会报错。③ 它的降级规则同样值得抄：匹配不上的子记录**独立发一次而不是丢弃**——去重宁可留重复，不可丢事件（`:51-65`）。落地前先确认时间窗在我们的判据下是必需项还是可去掉 |
| **G6** | 功能缺口 | 🟡 | **判据 fixture 是照「见过的形态」手搭的，没见过的第三种形态对测试不可见**——`tools/fixtures.jsonl` 26 条覆盖两种拒绝正文（`Permission to use …` 六个变体含多行命令 + `The user doesn't want to proceed …`），这次核过没有镜像盲区；但**机制上挡不住新形态**。三方实测的反例值得警惕：teamai-cli 的判据测试与我们同级完备（13 个测试文件涉及判据，含一条把两个 interrupt 变体钉成预期的用例），却因全套件里 `Permission to use` 出现 **0 次**，对它自己 52% 的人拒漏判**恒绿**——测试不是缺失，是靠 fixture 选择恒绿。这正是 [CAPABILITIES §2.2](CAPABILITIES.md) 记的「假绿」模式。要动：fixture 来源从「见过的」换成「从全语料聚类出的 `is_error` / interrupt 正文形态」。**扫完 LoongSuite Pilot 后结论不变：业内仍然没人解。** 它整个仓里没有任何聚类或语料抽样方法；唯一沾边的是一个**方向性**证明——`docs/codex-aborted-turn-recovery.md:11-12,64-67` 判「用户打断」用的是类型化记录 `event_msg:turn_aborted` 而**不是** UI 字符串，输出再归一到固定词表。也就是说：**能拿到类型化信号的 harness 就不该匹配字符串**。Claude Code 的 transcript 在这一点上不给类型（`[Request interrupted by user` 只有正文），所以我们和它们一样被迫吃字符串——但这条提醒值得记：每次上游更新都该先查一遍有没有新增的类型化字段可以替换掉硬编码串。**2026-09-15 补了一道字段哨兵**：被拒记录的 `toolUseResult` 是 `User rejected tool use`，与正文判据是两个独立字段，映射账本记「有标记而判据没认出」的条数（本机 45 个标记、0 次漏判），判据漂了会先在这里露头；机制上仍挡不住两边一起变的新形态。**09-16**：D13 去掉 PermissionDenied / StopFailure 这类 hook 之后，DESIGN §3.2 设想的「hook 事件流与字符串判据对账」这道哨兵用不上了，剩下能做的是语料里「已知清单之外的记录类型 / attachment 类型」告警（TODO 完整性钉子）。**09-16 第二批做了**：映射账本按记录类型、附件类型、system 子类型计不认识的，doctor 点名；09-17 拿本机 20 个会话真实补采，报出 `auto_mode_exit`（只有两个开关字段），已登记 |
| ~~**G10**~~ | 功能缺口 | ✅ | **已钉**（2026-09-17，`03847f8`）：test-hook-flow 第 22 节钉住一个会话扫到哪些 transcript——主会话、一级子 agent、被子 agent 派出的、workflow 起的，四份各有 state、各出 spool 块，`journal.jsonl` 与 meta 不当 transcript（撤掉递归扫描 5 项红）；第 1 节钉未登记的仓零写入，第 16 节钉登记的多个仓与删掉的 desktop worktree 留下的会话照样补。运行时另有 A11 的恒等式（每份 transcript 新读到的行都要有去处，按文件累计进 `integrity.json`，doctor 汇总）。原记录：**采集范围没有回归钉子**——判据有 `tools/test-extract.sh` 守着，「扫过哪些文件」**一条断言都没有**。现状是对的（`vibetrail-sync` 按 `git worktree list` 聚合，实测覆盖 762 个 transcript = 主会话 34 + 子 agent 728），但没有任何测试会在它变窄时失败：范围一缩，判据测试**全绿**，数字静默减半。（2026-09-14：`vibetrail-sync` 退役，钉子改钉 G7 的门控与增量副本范围，已写进 DESIGN A11。）这是 G6 的姊妹问题——G6 是判据盲区，本条是范围盲区，且**本条更隐蔽**（G6 至少会在新形态出现时错，本条连错都不会报）。三方实测的反例：teamai-cli 的判据方法论与我们同源，实际差距的一多半却来自「只扫了 hook 递来的那一个文件」这种与判据无关的地方（[对比 §3.2 / §7 第 2 条](third-party/teamai-cli-vs-vibetrail.md)）；LoongSuite Pilot 是反过来的样本：它装了 `SubagentStop`，**提取层还有测试钉着**——`tests/unit/hooks/claude-code/hook-processor.test.mjs` 的「claude-code 一级子 Agent 上报」一组用例（`:725` 起）断言导出记录里必须有 `gen_ai.agent.scope = subagent`，删掉子 agent 展开就红；它缺的是部署层，没有测试断言 `agents.d/claude-code.json` 必须注册 `SubagentStop`（Codex 那边有，`tests/unit/hooks/codex/hook-processor.test.mjs:52-61`）。三方里我们和 teamai 两层都没有。Pilot 还顺带给了一个**文件级钉子抓不到的反例**：它扫到了文件，但解析器按 promptId 分组，把轮末的中断记录静默丢了——用它自己的解析器离线跑本机语料，265 条中断记录只有 5 条进了事件，它的子 agent 测试照样全绿（[采集清单 §1.2b](third-party/loongsuite-pilot-collection.md)）。所以钉子不能只钉「扫了哪些文件」，还要钉「每类记录走到输出的条数」。**要动需要什么**：LoongSuite Pilot 有三种钉子分别对应三种成因（[对比 §6.2b](third-party/teamai-cli-vs-vibetrail.md) 有摘要，行号以本条为准）：① **改代码改缩了** —— `tests/performance/trace-runtime.perf.mjs` 用 esbuild 插件把 import 重定向到 `git show <baseline>:<path>`，baseline 与 modified 各打一个包，同一份输入在独立子进程里轮流跑（`:114`），要求两边的 `eventDigest` / `traceDigest` 两个 sha256 与条数完全一致（插件 `:98-104`，比对 `:124-125`；`:113` 按 `run % 2` 交替先后顺序，消除预热偏差）；② **运行时环境变了** —— `input-runtime-metrics` 四阶段漏斗（`raw_read_*` → `raw_in_*` → `parse_*` → `in_events`），105 行纯标量、payload-free、维度不含 session_id 与文件路径所以基数不随数据量增长；③ **I/O 静默吞掉** —— 要求扫描器返回显式的 scan-completeness 信号，「空结果不等于删除」（`docs/agent-onboarding.md:270-274`）。**对我们最便宜的是 ①**：钉一份语料，HEAD 与基线 ref 各跑一次提取器，diff 的不是判据结果而是**扫过的文件路径排序后的摘要**（`find \| sort \| sha256sum` 即可），范围一缩摘要立刻变；② 等 G7 落成 hook 之后再说。⚠️ 抄的时候注意：Pilot 那条漏斗恒等式（`parse_success + parse_failed = raw_in_records`）在它 `tests/` 里 **grep 不到断言**，只是文档口径——我们要做成断言，别把「写在文档里」当成「守住了」 |
| ~~**G4**~~ | 功能缺口 | ✅ | ~~没有留痕自检~~ —— **已实现** `old/vibetrail-doctor` |
| **G7** | 需求 | 🔴 | **hook 采两路数据（全量 + 人机分歧），HTTP push 云端，机器级装一次，被观测仓零写入**——2026-09-14 定稿为 [DESIGN.md](DESIGN.md)（用户原话 §0、方案 §2–§5、决策 D4、验收 §9），拆解在 [TODO.md](TODO.md)，未定项拆成本表 U1–U10；2026-09-15 D5 定了采什么与去哪（不传 transcript 原文件、云端定协议 1.0），关 U3 / U10 / K1。同日做完拆解第 1 步（提取器扩展与协议映射，`tools/map-events.jq` + `tools/vibetrail-map`，DESIGN §4.2），对照 Pilot / teamai 补强（回放副本不上报、嵌套子 agent 父实例、斜杠命令、合成记录、requestId 兜底、字段哨兵）并定 U11；随后分歧一路挂上 hook（`tools/vibetrail-hook`：门控、会话锁、state、spool 块文件，回归 `tools/test-hook-flow.sh`）。同日接着做完 hook 分发入口的其余事件（会话 / 轮次 / 子 agent 起止、`ext.claude.*` 事件头、git 状态）、轮次元数据（turn.end 在模型答完时发，Stop 时当场关轮，DESIGN D7）、commit ↔ 轮次推导、机器级安装与本地查看（`tools/vibetrail` init / uninstall / projects / list / show / doctor，沙箱演示 `experiments/collect-demo/demo.sh`）；分歧与轮次元数据是同一条事件流。**push 与补充回归按用户 09-15 的要求往后放**（「push，回归这些都先不急着做」）。过程记录（2026-09-09 提出、09-11 定两路、09-14 七次追加，含 teamai / Pilot 对照要点）在 git 历史里 |
| **G8** | 需求 | 🔴 | **「采集能限制在指定项目，就像 teamai 一样」**（用户原话，2026-09-09）。**现状（D4 之前）**：`vibetrail-sync` 按 `git worktree list` 只认领本仓的会话，天然按仓隔离；但没有「这台机器上哪些项目开了采集」的开关，G7 落成 harness hook 之后尤其要有——hook 写在 HOME 的工具设置里就是全局的。**teamai 的做法**（[分析文档 §4.3 / §4.4](third-party/teamai-cli.md)，09-09 对照源码复核后改）：hook 挂在 HOME，`hook-dispatch` 只用传入的 `cwd` 选 config、**不做门控**——config 取不到时注释自称 fail-open，19 条 handler 照跑，`dashboard-report` 在没 init 过的目录一样把事件写进本机 `events.jsonl`（`src/hook-dispatch-cli.ts:185`、`src/hook-handlers.ts:503-517`）；「限定」只发生在上报环节，`filterEventsByScope` 按 cwd 是否在 projectRoot 下决定哪个团队仓收哪些会话（[采集清单 §1 / §3.1](third-party/teamai-cli-collection.md)）。也就是说 teamai 有的是「上报限定」，不是「采集限定」，第一版写的「其余目录一律早退」不成立；分区键用 `git worktree list` 第一条当稳定身份。**要动需要什么**：一份机器级的项目清单（按主 checkout 分区，worktree 共享），分发入口先查清单再干活，`doctor` 报「本仓在不在清单里」。**先记录、暂不定**：清单粒度（仓 / 目录 / 分支）与开关方式（install 时登记还是单独命令），等需求和功能完善后再定。**09-14 随 G7 定了一半**：scope 可配——`project` 时机器级清单是开关、未登记不采；`user` 时全机采、用户显式选。hook 条目只在 HOME，仓里不留痕。登记方式已立 U2，默认 scope 已立 U1 |
| **G9** | 需求 | 🔴 | **「要在本地能展示一份采集的内容，让开发放心没有侵犯隐私」**（用户原话，2026-09-09）。**现状**：查询端 `vibetrail show / log / session / diverge` 读的是同一份数据，但没有面向「我被记了什么」的视图；而 K6 已证明 trace 里确有自由文本（`claim` / `desc` / `cwd`），先展示出来的会是问题本身。**teamai 的做法**（[分析文档 §3.3](third-party/teamai-cli.md)、[对比 §6.1](third-party/teamai-cli-vs-vibetrail.md)）：`session save` 先在本地落一份脱敏摘要，团队推送默认只推计数与工具名，prompt 文本要显式 `--include-prompt`；dashboard 只在本地起；文档明写「只统计次数」（本地事件流其实是明文，见对比 §6.1）。**要动需要什么（D4 之后改写）**：一条命令把「即将 push 的内容」原样列出并明说什么出了本机——现在什么都采，prompt 正文、thinking、代码 diff 都在全量副本里（D2「只存指针」已被 D4 取代），`vibetrail push --list / --show` 开启前预览；脱敏 K6 暂缓，先原样展示、如实写「什么出了本机」。**LoongSuite Pilot 在这条上没有完整答案，值得记下来免得下次再去翻**：它有本机 Dashboard（`127.0.0.1:8765`，只读 `logs/metrics-summary.json`）和 `input-runtime-metrics` 四阶段漏斗，但两者回答的都是「**多少**」（Dashboard 是 token、会话、请求、工具调用、模型与仓库的用量汇总，漏斗是读了几次、几条、产出几个事件），**不是「里面有什么」**。它文档化的「看」是一份默认开的本地 JSONL 副本：`docs/zh-CN/overview.md:91` 说没配远端时 JSONL 默认开启、「方便本地验证采集是否生效」，`docs/zh-CN/sls-output.md:158-162` 教的是 `tail -f ~/.loongsuite-pilot/logs/output/*.jsonl`——能看，但得自己翻原始事件，没有任何命令或视图按「我被记了什么」汇总；想少暴露，文档给的路是 `captureMessageContent: false` 让它**不采**，再加脱敏（`docs/zh-CN/masking.md:7-12` 建议两者同时用）。所以 G9 要的那种「给开发看、让开发放心」的视图在两家三方实现里都没有先例，我们要自己设计；可借的只有分层展示的口径（teamai 的「计数 / 截断文本 / 路径类信息」三分法，见 [teamai 采集清单 §6](third-party/teamai-cli-collection.md)）。**先记录、暂不定**：展示形态（CLI 表格 / markdown / 本地网页）与是否连带展示 commit trailer，等需求和功能完善后再定。**2026-09-14 前提变了**：G7 定了本机不存档、ack 即删（DESIGN D4），端点配置后本地没有内容副本可展示——本条改为「看发出去的清单与元数据 + 开启前预览」，内容看云端；commit trailer 已随 git hook 退役，不再有可展示的。**2026-09-15 再缩**：「读取不是我们读，我们只负责采」，云端展示由别的服务做；本条只剩 push 前的本地预览（`vibetrail push --list / --show`），且 D5 后出本机的正文只有分歧事件那几段，预览量很小。**09-16 前提又变了**：用户定全采正文，出本机的正文几乎是全部，「哪些正文出了本机」按条列没有意义；预览改成按事件类型汇总条数与正文大小、能抽看某一条（TODO 里 `show --bodies` 的原设想作废），`push --list / --show` 随 push |
| **G11** | 需求 | 🔴 | **多个会话改、最后一个会话提交时，追回每一行出自哪个会话的哪次工具调用，再回到对话里判断是模型错还是人错**（用户 2026-09-10 提出）。已有方案与 demo，审过六轮；第六轮之后主方案改为「worktree + 规矩 + 接手时拍一次快照」，逐次工具调用快照降为可选（它的 7 处已知错未修）。未开工；下一步：agentDock 先装 trailer，再用重写后的测量脚本量多会话 commit 与接手的频率。快照是我们自己挂还是改造 Pilot、对话证据靠上传还是定期存 transcript，先记录、暂不定。排在 G7 的两路采集之后（用户 2026-09-11）。需求原话、方案、验证、限制、拆解见 [TODO.md](TODO.md) |
| ~~**D1**~~ | 待定决策 | ✅ | ~~归属语义三处不一致~~ —— **随 `Claude-Session` trailer 退役而消失**（2026-09-14，D4）：commit ↔ session 改从每轮起止 HEAD 推（DESIGN §3.5），归属语义在那里重新定义 |
| ~~**D2**~~ | 待定决策 | ✅ | ~~SpecStory 留不留人类可读副本~~ —— **不留**（2026-09-14）：全量副本上云后没有第二份副本的位置；SpecStory 在 DESIGN §7 已否 |
| ~~**K7**~~ | 已知缺陷 | ✅ | ~~按停止打断正在跑的工具，被记成「人拒绝了这次调用」~~ —— **已修（09-15，用户：「K7 按 permissionMode 先粗分，挂上 PermissionRequest」）**：挂上 PermissionRequest 记权限框证据；映射层对「The user doesn't want to proceed…」二次判定——弹过框是拒绝，没弹过、子 agent 里的 User rejected tool use、没挂上时 auto / bypassPermissions / dontAsk 模式下的，都判成按停止，发 `turn.end(interrupted)`、kind `interrupt_tool`（DESIGN D9，spec/diverge-v1 §2.3）。两家都分不开（teamai 同时算一次拒绝加一次打断，Pilot 一律 ToolError）。**待实测项 09-16 关闭**：装上后本机 8 条 `ext.claude.permission_request`，含子 agent 里的（带 `agent_id`）、auto 模式下 AskUserQuestion 的、acceptEdits 下 Edit / Bash 的——desktop 里确实触发，payload 与二进制一致 |
| ~~**K8**~~ | 已知缺陷 | ✅ | **已修**（2026-09-16，`51f8716`）：映射层回放副本那一处加「记录 `sessionId` 与文件 sid 不同就整条跳过、账本记 `inherited`」，照原记录的修法；test-map 第 7 节回归、变异测试有效。原记录：~~**desktop 续接会话时复制的历史，会按新会话再报一遍**~~（2026-09-15 用 `vibetrail sync` 补采本仓旧会话时发现；**09-16 复核升 🔴**：trace 上线后量级变大，修法也找到了，见末尾）。`aeb3a412`（811 行）的开头被原样复制进了 `5919909a`（347 行）与 `b032681b`（317 行），两个都在另一个 worktree 里：记录 uuid、promptId 完全相同，只有会话 id 换了。event_id 按「会话 id + 记录 uuid」算，所以同一次打断、同一次拒绝、同一批轮次在三个会话里各报一遍（本机这次：分歧 11 条里 3 条是复制来的）。回放副本（同一文件里再追加旧记录，DESIGN §4.2）处理的是另一种情况。演示报告（`experiments/collect-demo/report.jq`）已先按记录 uuid 合并显示。**09-16 本机 124,666 条真实事件复核**：60 个 turn_id 同时出现在两个会话里（`0626f04d` + `9b94a81d` 47 个、`04609bdf` + `8641d4dc` 7 个、`8c9eb046` + `a7584db0` 6 个），落在上面的事件 9,448 条（7.6%），其中 `message.assistant` 3,989、`tool.end` 5,016——被复制那段历史的每次模型调用、工具调用云端都算两遍，token 直接翻倍。**根因与修法**：复制进新文件的每条记录，`sessionId` 字段仍是原会话的（`9b94a81d` 的文件里 5,527 条记录是 `0626f04d` 的、自己的只有 148 条；`8641d4dc` 461 条、`8c9eb046` 359 条同理），所以在映射层回放副本那一处（`tools/map-events.jq` 的 `step`）加一条「记录 `sessionId` 与文件 sid 不同就整条跳过、账本记 `inherited`」即可——不需要机器级 uuid 索引，也不用看 fork；子 agent 文件里 `sessionId` 是父会话 id、与 `--sid` 一致，不受影响。原先写的「机器级记下已发过的 uuid 与 promptId」作废。回归补一份带外来 `sessionId` 记录的 fixture |
| ~~**K9**~~ | 已知缺陷 | ✅ | ~~内部 agent 也触发 SubagentStart / SubagentStop~~ —— **已修（09-15）**：二进制里内部 agent（压缩、起标题、提示建议……）的 `agent_type` 为空、没有 SubagentStart、transcript 路径不存在；`agent_type` 为空的只记 `ext.claude.subagent_stop`（internal），不发 `subagent.end`（DESIGN D10）。两家都碰不到这个问题。**09-16 起不挂 SubagentStart / SubagentStop**（D13），子 agent 起止从 transcript 推，内部 agent 没有 transcript 文件、不再有记录，这条问题随之不存在 |
| ~~**K10**~~ | 功能缺口 | ✅ | ~~干活时插进来的话不算「之后人说的话」~~ —— **实测不成立，另补插话计数（09-15）**：本机 14 次分歧之后人的下一句 9 次都是正常人话、0 次插话——主会话里拒绝与打断都当场结束这一轮，没有「模型还在跑时插话纠正」的机会。插话本身以前完全没记，改在 `turn.end` 上记 `vibetrail.queued_prompts`（只计数、不带正文，DESIGN D10）。两家都把插话丢掉。插话当作分歧信号（spec §2.2 的 `absorbed_mid_turn`）仍归 U8 |
| ~~**K11**~~ | 功能缺口 | ✅ | **已修**（2026-09-16 `6cb3786` / `f81ff53`，09-17 `03847f8` 补端到端）：09-16 用 Workflow 实测了形态——workflow 起的 agent 写在 `<sid>/subagents/workflows/<runId>/agent-<id>.jsonl`，meta 没有 `toolUseId`、有 `workflowPhase`；同目录 `journal.jsonl` 按 agent 记 started / result，`<sid>/workflows/<runId>.json` 里有 taskId；主会话里 Workflow 调用的启动结果带 `taskType` = local_workflow、`runId`、`taskId`，跑完来一条 task-id 是 taskId 的 `<task-notification>`。做法：hook 递归扫 `subagents/`（最多往下 3 层）；workflow agent 的派出调用按 runId 从主会话的启动结果里找（存进 agents.json 的 `workflows`），跑完没有看 journal；主会话收到通知时，给这次 run 里跑完的每个 agent 发 `subagent.end`（父实例 main、`parent_call_id` 是那次 Workflow 调用，带 `vibetrail.workflow` 的 run_id / phase / label），workflow agent 自己的 `subagent.start` 也带 `vibetrail.workflow`。回归：test-map 第 9b 节、test-hook-flow 第 22 节。原记录：**workflow 子 agent 的 transcript 不在 `subagents/` 这一层，没解析**（2026-09-15 Pilot 核对读二进制发现）。workflow 起的子 agent 写在 `<sid>/subagents/workflows/<runId>/` 下，`process_session` 只扫 `<sid>/subagents/agent-*.jsonl`；SubagentStop 的结束标记当时改用了 payload 的 `agent_transcript_path`——**09-16 起不挂 SubagentStop**（D13），这条路用不上了。本机没有样本，先不改；遇到了改成递归扫 `subagents/` 下的子目录，父子关系照旧取 meta.json 的 `toolUseId`，「写完了」照旧看父文件里的完成信号 |
| ~~**K12**~~ | 已知缺陷 | ✅ | **已修**（2026-09-16，`51f8716`）：只在人话或斜杠命令处开轮，续接摘要、`<task-notification>`、只有 tool_result 的记录并入当前轮；回归在 test-map 第 7 节。老版本「整会话一个 promptId」的兜底没做。原记录：~~**非人话的 promptId 也被当成一轮，`turn.end` 的 `unknown` 被灌水**~~（2026-09-16 复核本机 spool）：turn.end 2,389 条里 status `unknown` 154 条（6.4%），其中 106 轮一次模型调用都没有；抽样是 `/auto-mode-setup`、`/compact` 后的「This session is being continued」续接摘要、`<task-notification>`、只有 tool_result 的 user 记录——各带一个新 promptId，就被 `turnBoundary`（`tools/map-events.jq`）开成一轮，transcript 补位的 `turn.start` 也跟着发（2,380 条），而 hook 侧的 `turn.start` 只在 UserPromptSubmit 发，两边「轮」的口径已经不一致。要动：只在 `isHumanPrompt` 或斜杠命令的记录处开轮，其余 promptId 的记录并入当前轮；退一步至少在 turn.end 上打 `vibetrail.turn_kind`（human / slash / notification / tool_result）让读的一方能过滤。剩下 47 条真 unknown（有用量、没 end_turn、没 summary）保留。另有一个老版本的边角：2.1.237 的会话 `1e0eb682` 整个会话只有一个 promptId（30 句人话），老版本的 promptId 不是「每句人话一个」，只影响补采历史，兜底可用「promptId 从不变化时按人话切」 |
| ~~**K13**~~ | 已知缺陷 | ✅ | **已修**（2026-09-16，`51f8716`）：打断的 turn.end 补 `vibetrail.closed_by` = interrupt 与 `vibetrail.stops`，打断后置 closed；「同 turn_id 多条取哪条」照原记录并进 U13。原记录：~~**同一轮可能出两条 `turn.end`、状态相反，读的一方没有取舍规则**~~（2026-09-16 复核，本机 4 组）：如 `1ae9404e` 的轮 `9e0d9a3e` 08-13 06:43 被 summary 关成 `completed`，08-14 02:21 同一 promptId 又来一条打断记录，再发一条 `interrupted`——两条 event_id 不同，都会上云。根因：`interrupted`（`tools/map-events.jq`）不看 `.pturn.closed`；打断发的 turn.end 也不带 `vibetrail.closed_by` / `vibetrail.stops`（本机 252 条全空），连 D7 定的「同 turn_id 取 stops 最大」都用不上。同类：hook 的 `subagent.end`（SubagentStop，永远 completed）与 transcript 的 `subagent.end(cancelled)` 也是同一实例两条。要动：打断的 turn.end 补 `closed_by` = interrupt 与 `stops`，打断后置 `closed`（顺带修掉「打断结束的轮没置 closed、每次 SessionStart 补做都重新解析一遍」，本机 5 个）；「同 turn_id 多条取哪条」并进 U13 与 collector 一起定 |
| ~~**K14**~~ | 已知缺陷 | ✅ | **已修**（2026-09-16，`4235fca`）：uninstall 只删 bin、logs 与 state 里除 `ids` 之外的内容；回归「装 → 补采 → 卸 → 装 → 补采，spool 条数不变」在 test-hook-flow 第 17 节，变异测试有效。原记录：~~**uninstall 删 state 留 spool，重装后 spool 翻倍**~~（2026-09-16 复核 `tools/vibetrail` 的 uninstall）：`state/<sid>/ids`（已写进 spool 的 event_id）随 state 一起删，spool 留着；重装后 SessionStart 补做把老会话整个再生成一遍，spool 里出现同 event_id 的两块，`list / show` 的数字翻倍（云端按 event_id 幂等，只是多发）。要动：ids 随 spool 留（不删 `state/*/ids`），或重装时从现有块重建 ids；回归补「uninstall 后重装不翻倍」。push 上线前先修 |
| ~~**K15**~~ | 已知缺陷 | ✅ | **五处全修**。**② 已修**（2026-09-16，DESIGN D13 同批）：`tool.end` 标 `vibetrail.duration_kind`——工具自报的耗时（WebFetch `durationMs`、WebSearch `durationSeconds`、Agent `totalDurationMs`，一条记录只夹一个结果时）优先、标 `reported`，否则是记录时间差、标 `wall_clock`，不设阈值、不丢值（用户 09-16「可以，就按这个做」）；`vibetrail show` 按来源注明。**①③ 已修**（2026-09-16，`4235fca`：capabilities 补 `tool.end`；补做时删「30 天没动且 spool 里没有待发块」的会话 state 目录）。**④ 已修**（`bbca2fd`，方案 A：拒绝的判定第一次判出就存进 `state/<sid>/splits.json`、重读沿用不重算；挂载改记时间段；另补记两种同类触发——弹框证据两周过期、uninstall 删证据）。**⑤ 已修**（DESIGN §4 体积按全采实测重写）。（② 当时的调研：Pilot 不算耗时、只发调用与结果两个时刻，teamai 不记工具耗时，Claude Code 只给 WebSearch / WebFetch / Agent 记了真实耗时。） **09-16 复核核出的几处实现与设计脱节**，各一行：① `session.start.capabilities`（`tools/hook-events.jq`）没有 `tool.end`，D8 加 trace 时漏了；② `tool.end.duration_ms` 按「结果时间减调用时间」算，本机 53 条超过 1 小时、最大 64 小时——后台工具、续接后才落盘的结果都算了墙上时间，超阈值应打 `vibetrail.duration_kind: wall_clock` 或不填；③ `state/<sid>/` 只清 turns / perms（`tools/vibetrail-hook`），main.json / ids / seen 永不清，transcript 30 天被清后它们还在；④ `permission_request_since` 只记一个时刻，判定用的是现在的配置值而不是事件发生时的，重跑 init 不登记 PermissionRequest 会把它清空，重读历史会翻旧事件的结论；⑤ DESIGN §4「体积每会话 KB 级」已不成立（见 U5） |
| ~~**K16**~~ | 改进 | ✅ | **已做**（2026-09-16，`4235fca`）：条目命令改成 `sh '…/vibetrail-hook' <事件> 2>/dev/null \|\| true`（新老写法都认、重跑 init 换掉）；stdin 流式读、1 秒空闲就当读完（实测不关 stdin 时 1.1 s 退出）；回归在 test-hook-flow 第 17 节。原记录：~~**hook 命令串与 stdin 读取的两道保险（借 teamai）**~~（2026-09-16 对照 teamai 源码；**随 D12 的 sh 包装一起做**，命令串改成 `sh '<路径>/vibetrail-hook' <事件> 2>/dev/null \|\| true`，stdin 在 node 里读、天然可设超时）：① 条目命令写成 `/bin/bash '<路径>/vibetrail-hook' Stop 2>/dev/null \|\| true`（teamai `builtin-hooks.ts:191` 的写法）——运行时被删、脚本 stderr 冒错时，Claude Code 不会在每个事件上弹 hook 错误；要同步改 `settings_ok` 与 doctor 的命令解析；② `vibetrail-hook` 读 stdin 是裸 `cat`，宿主不关 stdin 时同步 hook 会挂满 10 s 的 timeout，teamai 用 1 s EOF 保护（`hook-dispatch-cli.ts:36-65`），bash 里用 `read -t` 循环即可 |
| ~~**K17**~~ | 已知缺陷 | ✅ | **已修**（2026-09-16，`a34bbf5`）：`project_id` = 登记表里显式写的（`projects add --name`）> origin 仓库名 > 主 checkout 目录名（`tools/lib/hook.mjs` 的 `vtProjectName`）；`workspace_id` = 第一次见到主 checkout 时生成、持久化在 `~/.vibetrail/workspaces/<sha16>` 的 UUIDv4（`vtWorkspaceId`，`wx` 写入防两个 hook 同时生成），按主 checkout 一个（③ 取倾向，接 collector 时确认），uninstall 留着、`--purge` 才删；映射器没给 id 时按第一条记录的 cwd 用同一套算；doctor 报两个标识。回归：test-hook-flow 第 2 / 16 / 17 节（origin `git@github.com:acme/second.git` → `second`、没远端 → 目录名、卸载重装 id 不变）。本机路径现在只在 extensions 的 `vibetrail.worktree` / `vibetrail.cwd` 与 `ext.claude.*` 的 payload 里（D11 定的，K6 脱敏范围）。原记录：~~**`project_id` / `workspace_id` 不合采集端协议，push 前必须改**~~（2026-09-16 对照 `paas-coding-collector/docs/collection-event-protocol.md`「项目和工作区标识」核出）。**在哪**：`tools/lib/hook.mjs` 的 `setProject`（`project_id` = origin 去掉协议、用户名与 `.git`，没有 origin 用主 checkout 路径；`workspace_id` = 主 checkout 路径），`mapFile` 没给时取第一条记录的 cwd。**现状**：本机 spool 125,083 条事件里 `project_id` 是 `github.com/…` 这种主机加路径的形态，`workspace_id` 全是带用户名的本机完整路径。协议要 `project_id` 是简单可读的项目名（优先级：显式配置 > git remote 的仓库名，如 `git@host:team/payment-service.git` 取 `payment-service` > 工作区根目录名），不得用含用户名、token 或完整本地路径的值；`workspace_id` 是不透明的稳定字符串，工具不给就由客户端第一次见到工作区时生成 UUID 并持久化，collector 不解析它。**为什么要在 push 前**：collector 按 `user_id + workspace_id + agent.name + session_id` 关联会话，推出去之后再换 id，同一个仓的历史在云端分成两份。**要动**：① `project_id` 取 remote 最后一段去掉 `.git`，没有 remote 取主 checkout 的目录名；② `workspace_id` 第一次见到时生成 UUID、持久化在 `~/.vibetrail/`，uninstall 像 `ids` 一样留着（重装后不换）；③ 先定一问：按主 checkout 一个（D11 的口径，worktree 共享），还是照协议字面「一个 ID 对应一个工作空间根目录」每个 worktree 一个（desktop 每开一个 worktree 就是一个新根目录，会碎）——倾向按主 checkout，接 collector 时确认；④ test-hook-flow 第 16 段钉着现在的取值，跟着改 |
| ~~**K18**~~ | 已知缺陷 | ✅ | **已修**（2026-09-16，`a34bbf5`）：`tool.end` 的 code 改 succeeded / failed / cancelled；分类 `error` 一律改 `failure`（`closeTurn` 的 StopFailure / API 出错、`agentEndStatus` 的 failed 等）；来源自己的状态（interrupted、denied、hook_stopped、rate_limit、killed）留在 code、靠分类聚合（要动里的「倾向前者」）。goldens 重生成，`show` / 演示报告新老 code 都认。原记录：~~**状态分类与 code 没用协议推荐值，push 前改**~~（2026-09-16 对照协议文档「状态语义」核出；U13 的状态部分由此有了答案）。**在哪**：`tools/lib/map.mjs` 的 `toolEnds`（`success` / `error` / `cancelled`，分类 `success` / `error` / `cancellation`）、`closeTurn`（StopFailure 与 API 出错是 `<错误类型>` / `error`）、`agentEndStatus`（failed 等是 `<code>` / `error`）。**现状**：本机 spool 里 `tool.end` 有 62,779 条 `success/success`、1,074 条 `error/error`。协议推荐的分类是 `success`、`failure`、`cancellation`、`denial`、`skipped`、`unknown`，不认识的分类 collector 按 `other` 统计——我们的 `error` 分类全会落进 `other`；推荐 code：`tool.end` 是 `succeeded` / `failed` / `cancelled` / `unknown`，`turn.end` 是 `completed` / `failed` / `cancelled` / `unknown`，`session.end`、`subagent.end` 是 `completed` / `failed` / `cancelled`。code 允许放来源自己的状态（协议的例子就是 `post_tool_use_failure` / `failure`）。**为什么要在 push 前**：云端按分类聚合，推出去的历史口径就定了。**要动**：分类一律换成推荐值（`error` → `failure`）；有推荐 code 的用推荐 code（`tool.end` 的 `success` → `succeeded`、`error` → `failed`）；来源自己的状态（`interrupted`、`denied`、`hook_stopped`、`rate_limit`、`killed`）二选一——留在 code、靠分类聚合，或者 code 用推荐值、来源状态进 `detail`；倾向前者（信息不丢，协议允许）。goldens 与断言跟着改 |
| ~~**K19**~~ | 已知缺陷 | ✅ | **已修**（2026-09-16，`a34bbf5`）：四路统一升到 v2 基线（`map.mjs` 的 `RULE_VERSIONS`，hook 侧 inferred 的 `turn-v2` 同源）；钉子在 test-map 第 10 节——把 fixtures 的 golden 与第 6～9 节的事件按 rule_version 算摘要记在 `expect/RULE-DIGESTS`，输出变了而版本号没升就红，`--update` 也红、只有 `--accept-rule-digest` 放行（确认只是 fixture 变了）；变异测试：改一处映射文案不升版本 → 红，升到 v3 → `--update` 收下新名字。原记录：~~**映射规则改了，`rule_version` 没升**~~（2026-09-16 对照协议文档核出）。**在哪**：`tools/lib/map.mjs` 里的 `diverge-v1`、`turn-v1`、`call-v1`、`ext-v1`。**现状**：09-15 定下之后，09-16 改了好几处规则——K8 跳过复制的历史、K12 只在人话处开轮、K13 打断的 turn.end 补字段、全采正文、D13 子 agent 起止 / API 出错 / CLAUDE.md / 切目录改从 transcript 推、K15② 耗时标来源——版本号都没动。协议：「适配器升级映射规则时更新 `rule_version`，不得改写已经接收的历史事件」。event_id 不含 rule_version，重读出来 event_id 不变、collector 按重复收下，所以升版本不会改写历史。**为什么没动**：还没推过任何数据，现在升不升对云端都一样。**要动**：第一次 push 之前统一升一次、定成基线（比如全部 `-v2`），之后每改一次映射规则就升；加一个钉子：golden 变了而 rule_version 没变，测试变红。**09-16 第二批又升**：diverge / turn / ext 到 v3（子 agent 文件、workflow、按 meta 的 toolUseId 认同步 agent 的结束、路径相对化），call 没变仍是 v2；测试里的版本号改成从 `map.mjs` 的 `RULE_VERSIONS` 读（09-17，写死的让 test-hook-flow 红了 8 项） |
| ~~**K20**~~ | 功能缺口 | ✅ | **已做**（2026-09-16，`a34bbf5`）：全采时每条 `queued_command`（commandMode prompt、origin 是人）发一条 `message.user`——`author_type` 沿用现有的 `user`（协议对人的推荐值是 `user` 还是 `human` 没有文档可核，接 collector 时一起确认）、`delivery` queued、正文取附件的 `prompt`、轮次按当前轮推（附件没有 promptId，本机 41 条全没有）、`_key` 用记录 uuid；不算分歧之后的下一句；关掉全采照旧只计数。本机这台 35 条。回归 test-map 第 9 节。原记录：~~**模型干活时人插进去的话（排队的 prompt），正文没上报**~~（2026-09-16 核出）。**在哪**：`tools/lib/map.mjs` 的 `isQueuedHuman` 只在 `turn.end` 上计 `vibetrail.queued_prompts`（K10）。**现状**：本机 25 份主会话里有 278 条。09-16 改成全采正文之后，人说的话里只有这一类没发出去。协议的 message payload 有 `delivery`，推荐值里就有 `queued`，并写明「不能把排队消息…当成人的实时输入」。**为什么没动**：K10 当时是「只带元数据」的口径，只计数。**要动**：全采时每条发一条 `message.user`（`author_type` = `human`、`delivery` = `queued`，正文取附件的 `prompt`，`_key` 用记录 uuid），不当成分歧之后的下一句；`capture_content=0` 时照旧只计数 |
| ~~**K21**~~ | 已知缺陷 | ✅ | **已修**（2026-09-16，`a34bbf5`）：`interrupted()` 给 `stop_calls` 里的每个调用补一条 `tool.end`（cancelled / cancellation，detail 写明是按停止打断，耗时按记录时间差标 wall_clock，`_key` 仍是 `<call_id>|tool.end`），只跑分歧一路时也发；判成人拒绝的仍只发 `permission.decision`。回归 test-map 第 9 节、test-hook-flow 第 14 节。原记录：~~**按停止打断正在跑的工具，只发了 `turn.end`，没发 `tool.end(cancelled)`**~~（2026-09-16 对照协议文档核出）。**在哪**：`tools/lib/map.mjs` 的 `toolEnds` 把「The user doesn't want to proceed…」一律当拒绝跳过；K7 判成按停止之后，`interrupted()` 只补发被打断调用的 `tool.request`。**现状**：协议「工具调用中断并导致轮次终止时，应分别发送 `tool.end(cancelled)` 和 `turn.end(cancelled)`……不能只保留其中一条」。结果是 `[Tool call did not complete` 那种已经发 `tool.end` cancelled，缺的只有 K7 判成按停止的这类。**要动**：`interrupted()` 给 `stop_calls` 里的每个调用补一条 `tool.end`（cancelled、分类 cancellation；`_key` 仍是 `<call_id>|tool.end`，之后真有结果也只留一条；耗时标 wall_clock）；判成人拒绝的仍不发（协议：执行前被拒只发 `permission.decision`） |
| ~~**K22**~~ | 功能缺口 | ✅ | **已做**（2026-09-16，`a34bbf5`）：Edit / MultiEdit / NotebookEdit / Write / Read 成功的结果记进当前轮（主会话文件才有轮），关轮时挂到 `turn.end.files[]`（打断结束的轮也带）：Write 的结果 `type=create` 记 create、其余改动记 modify、Read 记 read，evidence 一律 tool_result，同一文件取最重的操作；路径相对工作区根（hook 传主 checkout 与它的 worktree，最长的根先匹配；已删的 desktop worktree 去掉 `.claude/worktrees/<名>/` 前缀；`/private/tmp` 与 `/tmp` 同一处），根外的（K2 跨仓，本机常见的是 `~/.claude/memory/*.md`）不发、计在 `vibetrail.files_dropped.outside_workspace`，超 256 项先丢 read 再截断。先量了体积：本机 11 个会话 72 轮，有改动的轮平均 3.5 个文件、最多 10 个、路径约 100 字节，每条 turn.end 不到 1 KB。**没做**：子 agent 自己改的文件（它的 `subagent.end` 由父文件发，父不解析子 transcript）。回归 test-map 第 9 节。原记录：~~**`turn.end.files[]`（这一轮改了哪些文件）没做**~~（2026-09-16 对照协议文档核出）。**在哪**：映射层只出 `turn.end.commits[]`，没有 `files[]`。**现状**：协议把 `turn.end.files[]` 与 `commits[]` 当成查询「每轮改了哪些文件、对应哪些 Commit」的标准入口；文件关系的规矩 DESIGN §4.1 记过（`evidence=tool_argument` 只能是 `target` / `read`，说「改了」要 `tool_result` 或 `before_after`；路径相对工作区根、不能 `..`）。**为什么没动**：D5 定的两路（分歧、轮次元数据）没把文件列进去，之后没人提。**要动**：Edit / Write / MultiEdit / NotebookEdit 成功的结果（`toolUseResult` 带 `structuredPatch`、`type` create / update）记 modify / create、evidence `tool_result`；Read 记 read；跨仓的改动（K2）没有表达法，先不发。定下来之前先量本机一轮平均几个文件、多大体积。**09-16 第二批补子 agent 部分**（`6cb3786` / `f81ff53`）：子 agent 文件里没有轮，自己改读的文件记成整份文件的集合（新建靠结果正文「File created successfully」认——子 agent 文件里没有 `toolUseResult`），存进 agents.json 的 `files` / `files_outside`；它的 `subagent.end`（含被打断的）带 `files[]`，并进收到结束信号的那一轮 `turn.end.files[]`，被它派出的孙 agent 的文件也并进来。为此 hook 改成先映射子 agent 文件（深的在前）、再映射主会话、信号变了再补一遍子 agent 文件（K26 由此暴露） |
| ~~**K23**~~ | 已知缺陷 | ✅ | **已修**（2026-09-16，`a34bbf5`）：`agentStart` 只在全采时带 `task`、标 `content_state=included`，关掉时不带、标 `omitted`；断言在 test-map 第 8 节与 test-hook-flow 第 7 节。原记录：~~**`capture_content=0` 时 `subagent.start` 仍带 `task` 正文**~~（2026-09-16 D13 引入，当天核出）。**在哪**：`tools/lib/map.mjs` 的 `agentStart`。**现状**：`task` 取子 agent 的任务描述（meta.json 或后台启动结果里的 `description`），不看全采开关；协议把 `payload.task` 算正文，`content_state=omitted` 时必须缺失。**要动**：只在全采时带 `task`（标 `content_state=included`），关掉时不带、标 `omitted`；补一条断言 |
| ~~**K24**~~ | 已知缺陷 | ✅ | **已修**（2026-09-16，`a34bbf5`，取「要动」的第一种）：Stop hook 先等主会话文件末尾出现这一轮的答完标记（`stop_hook_summary`）或拦停反馈（`hook_blocking_error` 附件、「Stop hook feedback:」）再解析——有标记按标记关、拦停了不关，等不到才走 D7 的老路；上限 config `stop_wait` 默认 10 s，标记本来就在时不等。本机实测（52 轮）标记比最后一条回复晚 p50 1.9 s、最大 3.9 s（原记录「与 Stop 同一秒」是按 hook 触发时刻看的）。测试与演示 `VIBETRAIL_STOP_WAIT=0`；test-hook-flow 第 21 节按真实时序钉四种情形。「同一 turn_id 多条 turn.end 取哪条」仍在 U13。原记录：~~**被别的 Stop hook 拦下时可能先发一条 `turn.end`，与协议「不得提前发送」冲突**~~（2026-09-16 更新文档时对照协议文档核出）。**在哪**：DESIGN D7，`tools/lib/map.mjs` 的 `atEof`（`close_last` = stop）。**现状**：我们的 Stop hook 是 async、与别的 Stop hook 并行；拦停反馈（`hook_blocking_error` 附件、「Stop hook feedback:」）还没落盘时，第一次 Stop 就按 `hook_stop` 关轮、发一条 `stops` = 1，被拦下后第二次 Stop 再发 `stops` = 2 的一条（event_id 不同）——D7 当时有意这么设计，靠读的一方取 `stops` 最大的。协议：「Stop Hook 阻止 Agent 停止时，轮次仍在继续，不得提前发送 `turn.end`」；collector 虽然「使用最新可信 turn.end 覆盖已有汇总」，中间那条仍是提前发的。2.1.270 上 `stop_hook_summary` 与 Stop 同一秒落盘，本机 2,389 条 turn.end 里 `end_evidence: hook_stop` 一条都没有（D7 的 09-16 补记），实际多半走不到这条路，但机制上没挡住。**要动**：Stop 时等 `stop_hook_summary` 落盘再决定关不关（hook 是 async，多等几秒不耽误人，上限在 Stop 条目的 120 s 以内），等不到才走 D7 的老路；或者跟 collector 确认这种情况可以「后到的覆盖先到的」。**push 前定** |
| **K25** | 已知缺陷 | 🟢 | **DESIGN 没记「全采正文」这个决定，§0 / §2 / D5 还写着正文只随分歧事件走**（2026-09-16 更新文档时核出）。**在哪**：DESIGN §0「非分歧的消息与工具正文、thinking 都不传（§7 D5）」、§2 标题「分歧带正文，其余只有元数据」、D5 标着「现行」，§7 没有对应的决策条目。**现状**：用户 09-16 定「全采，prompt、输出、thinking、工具参数与结果 全文零截断 开、不脱敏」，默认开、`capture_content=0` 可关，已实现，写进了 DEMO §5 与 CAPABILITIES；当时用户说「design先不改，我可能要换语言」，DESIGN 只在 §2.2 个别行、§4 体积行与 D13 里零散提到。**要动**：补一条决策记全采（用户原话、放 payload 还是 extensions、只对新会话生效、默认开、超 1 MiB 去掉正文照发、system prompt 走扩展事件），改 §0 / §2 / D5 的现行说法 |
| ~~**K26**~~ | 已知缺陷 | ✅ | **已修**（2026-09-17，`03847f8`）：~~同一次 hook 里同一来源写两块 spool，后一块把前一块整个盖掉，前一块的事件就此丢了~~。**在哪**：`tools/lib/hook.mjs` 的 `vtSpoolWrite`，块名是「秒级时间-pid-来源」。**现象**：子 agent 文件在一次 hook 里会映射两遍（先写起止与调用，收到完成信号再补最后一次回答），两块同一秒、同一进程、同名，rename 覆盖了第一块；而第一块的 event_id 已经记进 `state/<sid>/ids`，那些 `subagent.start`、`tool.end`、前面几次调用再也补不回来。D13（`0d4fa7d`）起嵌套子 agent 会碰上；09-16 第二批（`f81ff53`）把「子 agent 先、主会话后、信号变了再补一遍」定成默认顺序之后，每次同步子 agent 结束都会碰上。09-17 写 test-hook-flow 第 22 节端到端时发现。**修法**：撞名时 pid 后面加 `_2`、`_3`（只有 cli 按文件名前 16 位取时间，不受影响）。**影响**：本机的 `~/.vibetrail` 现在没装运行时（卸载过），没受影响；装着 `9f0eab4` 的机器只在有嵌套子 agent 时丢。push 还没做，丢的只是本机 spool；要找回，删掉那个会话的 `state/<sid>/` 与它的 spool 目录再 `vibetrail sync`（event_id 是确定的，重采出来的一样）。回归：test-hook-flow 第 22 节（撤掉修复 2 项红） |
| ~~**U16**~~ | 待定决策 | ✅ | **已定（2026-09-16，DESIGN D13）：只挂 5 个 hook**——SessionStart / UserPromptSubmit / Stop / SessionEnd / PermissionRequest。用户先问「我可以在比如turn end的时候去transcript里面找到这些事件吗，不要单独给一个hook」，再要求把 13 个都过一遍、「可以，就这5个」。另外 8 个（SubagentStart / SubagentStop / PostToolUseFailure / Notification / PermissionDenied / StopFailure / InstructionsLoaded / CwdChanged）能给的都改在 Stop 时从 transcript 推；init 只登这 5 个、重跑清掉旧条目，没重跑的旧条目入口直接忽略，doctor 点名。代价：内部 agent 不再有记录，几个事件头没了，API 出错 / 按停止打断工具的轮 `turn.end` 晚到下一次 Stop。原记录：**登记事件的爆炸半径**（2026-09-16 立）：teamai 登记 4 个事件名、Pilot 4 个，我们最多 13 个（本机按三个版本的交集登了 9 个）。任何一个 Claude Code 版本删掉一个事件名，用户整份 `~/.claude/settings.json` 被跳过（连它自己的 permissions 一起），而本机心跳已否掉（DESIGN §7），只有 doctor 与云端「该有数据却没有」能发现。倾向：PermissionDenied / StopFailure / InstructionsLoaded / CwdChanged 这四个只记事件头的默认不登记，`--events all` 才加。**先记录、暂不定** |
| **K2** | 已知缺陷 | 🟢 | **跨仓归属未定义**——一个会话可跨多仓，本项目开发会话即反例（`cwd` 在 agentDock、commit 在 vibetrail）。2026-09-15：协议 1.0 的 `files[].path` 必须在 `workspace_id` 的根内、不能 `..`，跨仓改动没有表达法，已提第二轮意见（file 项可选 `workspace_id`） |
| ~~**G5**~~ | 功能缺口 | ✅ | ~~`tool` 字段未产出~~ —— **已关**（2026-09-15，协议映射落地）：映射层按 `is_error` 块的 `tool_use_id` 反查 `tool_use` 块的 `name` / `input`（`tools/map-events.jq`），反查不到退回 `Permission to use (\S+)` 捕获、再退 `unknown`，来路记在 `extensions.vibetrail.tool_lookup`（fixture `denials` 三种来路各钉一条）；原始命中多带 `call_id`（spec §1）。原记：协议 `permission.decision` 的 `tool_name` 必填，不用 regex（`The user doesn't want to proceed` 形态正文里没有工具名） |
| **K3** | 已知缺陷 | 🟢 | **空消息守卫假设 `core.commentChar` 为 `#`**——改了注释符的仓退化为不判空（不崩溃）。2026-09-14：这个 hook 只剩审计线在用，随 U6 一起定 |
| **M1** | ~~未量~~ | ✅ | ~~`is_error` 数组形态在语料里有多少~~ —— **已量（09-09）：1145 个 `is_error` 块全部字符串形态，数组形态 0 个**。铁律五的修复是纯防御，未恢复任何漏计 |
| **M2** | 未量 | 🟢 | **召回率绝对基线未测**——现有基线是无锚子串候选集；`permission_denied` 的「人工核对候选集」口径没记 |
| **M3** | 未量 | 🟢 | **`.meta.json` 字段集随版本变**——2.1.85 两项 / 2.1.202 三项 / 2.1.260 四项。全量副本逐字节复制它，消费方按缺失容错 |
| ~~**U1**~~ | 待定决策 | ✅ | ~~默认 scope~~ —— **已定**（2026-09-15，用户）：scope 可配置（`project` 只采登记的 / `user` 全机采），默认 `project`，参考 teamai；与 G8 一致，误采代价高于漏采（DESIGN §5） |
| ~~**U2**~~ | 待定决策 | ✅ | ~~登记方式：`vibetrail init` 在仓里跑时顺手登记 / `vibetrail projects add`~~ —— **用户 09-16 定**：「一个项目都不会加，等用户自己add，免得他在某个目录使用命令误操作加了」。init 不登记也不问；`projects pick`（从用过 Claude Code 的仓里选，编号前加 - 去掉）/ `add` / `remove [--drop]`（DESIGN D11） |
| ~~**U3**~~ | 待定决策 | ✅ | ~~全量一路的格式~~ —— **由 D5 关闭**（2026-09-15）：不传 transcript 原文件，不存 file-history；分歧映射成协议事件并带最小正文，其余只有轮次元数据（DESIGN §2、§4.1） |
| **U4** | 待定决策 | 🟡 | **端点、token、谁能看**。云端服务与 schema 已定（D5，2026-09-15）：paas-coding-hook 事件协议 1.0 的 collector，映射见 DESIGN §4.1。仍未定：端点地址；`Onepaas-Api-Access-Token` 怎么发与续期（hook 无人值守，要长期 token；**09-16 已做本机这一侧**：存 `~/.vibetrail/token` 0600，init 在终端里引导填、`vibetrail token` 另填，DESIGN §5；在哪里申请、过期怎么续仍未定，push 没做所以还没用上）；谁能看（协议默认全公司可见，与 K6 暂不脱敏冲突，已提第二轮 [意见](third-party/paas-coding-hook-protocol-feedback.md)）。端点没配之前 push 不发，可以最晚定 |
| **U5** | 待定决策 | 🟢 | **端点未配置阶段 spool 的上限与超限策略**。本机全量 700 MB 量级；倾向只警告不丢——采全（A2）优先于少存。配置后 ack 即删已定（D4）。**09-16 复核**：D8 加了调用 trace 之后体积前提变了——本机 45 个会话 120 MB、124,666 条事件，trace（`tool.end` 63,512 + `message.assistant` 54,431）占 95%，最大的会话 `5f069818` 约 50,000 条；DESIGN §4 的「每会话 KB 级」不成立，是 MB 级。spool 上限、push 门槛（D6）、云端 30 天索引的量都按这个重估。**09-16 全采正文后再测**（8 份真实 transcript 共 48 MB）：只带元数据 3 MB、全采 11 MB（约原文 1/4、只带元数据的 3.5 倍），单会话 0.5～3 MB；DESIGN §4 体积一行已按全采改 |
| **U6** | 待定决策 | 🟡 | **审计线去向**：`audits/` 仍在被观测仓里、仍靠 `prepare-commit-msg` 写 `Vibetrail-Id` 锚、闸门仍要仓内 vendor，与 D4「零写入」不一致。搬出仓要换锚（trailer 没了）与闸门的读法；不搬则 agentDock 保留自己那份 git hook 与 vendor。在定之前两条线互不影响（DESIGN §8、[spec/trace-v1.md](spec/trace-v1.md)） |
| **U7** | 待定决策 | 🟢 | **自建 hook 还是改造 Pilot**（同 G11）。用户 09-11「肯定不止靠pilot，我知道他做不到，要改造」；默认自建 |
| **U8** | 待定决策 | 🟢 | **上游类型化信号要不要成为新 kind**：`absorbed_mid_turn`、`edited_text_file`、`hook_blocking_error`、hook 事件 `PermissionDenied` / `StopFailure` / `Notification`（09-16 起这三个 hook 不挂，D13；API 出错从 transcript 的 `isApiErrorMessage` 推）。先按 [spec/diverge-v1.md §3](spec/diverge-v1.md) 的方式量精确率再定。**09-15 语料里又见三类没归类的机器拦截**（现在不发 `permission.decision`，不影响人拒统计）：`Auto mode could not evaluate this action and is blocking it for safety` 19 条（会话级安全检查）、harness 的 worktree 隔离拦截 `This agent is isolated in the worktree` 288 条、`Blocked: sleep N followed by …` 14 条；teamai 把它们算执行失败，Pilot 不区分。同样先量精确率再定加不加 kind。**09-15 看 teamai 时又见一种**：Stop 后 60 秒内的下一句含「不对 / 不是 / 错了 / 重来 / 撤销…」就记一次「纠正」（`CORRECTION_KEYWORDS`，teamai `src/types.ts`，6ae0619），是按人话关键词猜的弱信号，没有拿来当 kind；要不要做同样先量精确率 |
| **U9** | 待定决策 | 🟢 | **Codex / Cursor 要不要一起采**。默认不做：Pilot 三分之一代码在适配各家格式，地基（hook 热加载、`CLAUDE_CODE_SESSION_ID` 等于文件名）是 Claude Code 特有的实测 |
| ~~**U10**~~ | 待定决策 | ✅ | ~~云端保留期、传输层压缩格式~~ —— **由 D5 关闭**（2026-09-15）：索引保留 30 天够用（「超过一个月复盘意义不大」）；压缩待服务端支持 gzip（第二轮意见），D5 后体积 KB 级，不再是必需项 |
| ~~**U11**~~ | 待定决策 | ✅ | ~~映射每次从文件头重读的代价~~ —— **已定**（2026-09-15，对照 Pilot / teamai 后按建议定，用户随后让继续写）：UserPromptSubmit 不读 transcript，只在 Stop（异步）、SessionEnd、SessionStart 补做时解析；每次从本轮开头读（state 记 `checkpoint_line` / `checkpoint_byte`），106 MB 的会话一次从 10.5 s 降到 0.19 s；同一会话一把 mkdir 锁、Stop 里先等 transcript 写稳（照 Pilot）；不设 50 MB 上限（两家都设、都会丢分歧）。DESIGN §3.1、§3.3、§4.2 |
| **U13** | 待定决策 | 🟢 | **自定义取值待 collector 确认**（2026-09-15 立，实现轮次元数据时定的暂用值）：`turn.end` 的 status——`completed` / `success`（本轮开始后有过 Stop）、`denied` / `denial`（拒绝后停下）、`unknown` / `unknown`（没有证据）、`<错误类型>` / `error`（StopFailure）；`session.end` 的 status 填 `completed` / `success`；`session.start.capabilities` 暂填我们会发的事件类型清单；hook 事件头的类型名 `ext.claude.<事件名的 snake_case>`。协议只规定是 code 型、没给推荐值（`interrupted` 用户 09-15 说过可以直接加）。**先记录、暂不定**，接 collector 前一并确认，可并入第三轮协议意见（DESIGN §4.1）。**09-16 加一问**：同一 turn_id 多条 `turn.end`（D7 的 `|stopN`、K13 的关轮之后又打断）读的一方取哪条——倾向「`vibetrail.stops` 最大、再取 `occurred_at` 最晚」，要和 collector 定成一条规则。**09-16**：状态的推荐值采集端协议文档里已经有了，转成 K18 去改；剩下 `session.start.capabilities` 的清单、扩展事件的类型名、同一 turn_id 多条 turn.end 取哪条仍待确认（K24 也牵涉这一问） |
| ~~**U14**~~ | 待定决策 | ✅ | ~~turn.end 什么时候发~~ —— **用户 09-15 定：模型答完就算这一轮结束**（「模型完全回答完应该会有标记的吧。它不可能等下一个turn开始才知道结束」「那排查的时候就会缺失最后一个turn」）。做法：Stop hook 当场关轮（Claude Code 自己的答完标记 `stop_hook_summary` desktop 要等下一句人话才落盘，09-15 装机实测，不能等它），被别的 Stop hook 拦下后再 Stop 时补发一条 `stops` 更大的；拒绝停下在拒绝处关；打断没有自己的 hook，打断收尾的轮等下一个 hook（DESIGN D7） |
| ~~**U15**~~ | 改进 | ✅ | ~~trace 与人话识别两处可做的改进~~ —— **已做（09-15）**：每条 `system/api_error` 发一条 `ext.claude.api_error`（第几次重试、等多久、错误类型），按时间挂回那次调用的 `response_id`；有 `origin.kind` 就以它判人话（DESIGN D10）。两家都不采重试、不读 origin |
| ~~**U12**~~ | 待定决策 | ✅ | **已定、已改**（2026-09-16，`a34bbf5`，按「倾向」那版）：input = `input_tokens` + `cache_creation_input_tokens` + `cache_read_input_tokens`，cached = `cache_read_input_tokens`（input 的子集），reasoning = `thinking_tokens`（output 的子集），total = input + output，来源一个字段都没给的项不填；每次调用的 `vibetrail.call.usage` 同一算法；`show` 与演示报告列成「入（其中缓存）/ 出」。与 Pilot 的「token 全量公式」相同，teamai 的四项相加正是协议不许的。回归 test-map 第 2 / 9 节；本机 41 条 turn.end 全部 total = input + output、cached ≤ input。原记录：~~**token 口径**~~（2026-09-15 立）：协议 `usage.input_tokens` 包不包含缓存读 / 写，schema 看不出来。现在按「不含」映射：input = `input_tokens` + `cache_creation_input_tokens`，cached = `cache_read_input_tokens`（DESIGN §4.2）。teamai 在 Codex 那边专门把「input 含缓存」换算成分桶，说明各家口径确实不同。**先记录、暂不定**，接 collector 前跟服务端确认，可并入第三轮协议意见。**09-16 采集端协议文档给了方向**：「`usage` 是该轮来源可观测到的累计值，无法采集的字段应省略而不是传 `0`。`cached_input_tokens` 通常是 input 的子集，`reasoning_tokens` 也可能是 output 的子集，不能把所有字段直接相加」。我们现在 input 不含缓存读、total 是三项相加、缺的字段按 0 加，与这段不一致。倾向改成 input = `input_tokens` + `cache_creation_input_tokens` + `cache_read_input_tokens`、cached = `cache_read_input_tokens`（input 的子集）、total = input + output、reasoning 算在 output 里，来源没给的字段不填；`vibetrail show` 的「入/缓存/出」跟着改。**push 前定**（collector 用最新的 turn.end 覆盖汇总，推出去的口径就定了） |
| **U17** | 待定决策 | 🟡 | **push 门槛的默认值**（2026-09-16 立，做 push 前定）：D6 定的「全机最早待发超过 1 小时，或全机待发满 100 条」里，加了调用 trace 之后一轮常常就超过 100 条（本机 `message.assistant` + `tool.end` 占事件的 95%），等于几乎每次模型答完都推，D6 要的「每小时量级」不成立（TODO push 那条的 09-16 修正里记过）。两种：接受每轮都推，或者 `push_max_events` 默认调到 1,000 左右。另一问：SessionStart / sync 的后台补推如果限 10 批，本机积压的 1,245 批要 125 次 hook 才发完，倾向改成限时（如 60 s）不限批数。**先记录、暂不定** |

**已关闭**：O1 git-ai 采纳（否决）· O3 trace schema（已定）· O5 telemetry 配置（随 O1 消失）·
O6 存量 marker 迁移（不迁移）· O7 `core.hooksPath`（并入 G3）· A 节四组数字 · B 节五条决策 ·
M1 数组形态 · G4 留痕自检 · G3 的实现部分 · **G1 查询端** · **G2 session 投影** · **K5 跨 kind 重复** ·
K4 分歧记录含分支名（并入 K6）·
**L2 审计留痕**（`vibetrail-audit`，断链二解决）·
**G2 写入者 / G3 / D1 / D2**（2026-09-14 随 D4 关闭：hook 写 spool、机器级装一次、trailer 退役、不留 SpecStory 副本）·
**U3 / U10 / K1**（2026-09-15 随 D5 关闭：不传 transcript 原文件、30 天够用、父子打断是两个事实不去重）·
**U1**（2026-09-15 用户定：scope 可配，默认 `project`）·
**G5**（2026-09-15 随协议映射关闭：`tool_use_id` 反查 + regex / unknown 兜底）·
**U11**（2026-09-15 定：只在 Stop / SessionEnd / 补做里解析，从本轮开头读）· **U14**（2026-09-15 用户定：模型答完就发 turn.end，Stop 时当场关轮）·
**K7 / K9 / K10**（2026-09-15：按停止与拒绝分开、内部 agent、插话计数）·
**K8 / K12 / K13 / K14 / K16**（2026-09-16 复核修补，`51f8716` / `4235fca`）· **K15**（2026-09-16，五处全修）· **U16**（2026-09-16 用户定只挂 5 个 hook，D13）·
**K17 / K18 / K19 / K24 / U12**（2026-09-16 push 前对齐采集端协议，`a34bbf5`）· **K20 / K21 / K22 / K23**（2026-09-16 全采与协议补齐，同一提交）·
**K11 / G10 / K26**（2026-09-16 第二批与 09-17 收尾：workflow 子 agent、扫描范围钉子、同一次 hook 里 spool 块互相覆盖，`6cb3786` / `f81ff53` / `03847f8`）。


## D. 三轮审计的方法教训

- **自审一条高危都没抓到，三条全是独立 agent 用实验抓的。** 自己改过的东西看不出盲区；
  「修完再起一个新 agent 从头审」应当成固定流程。
- **hook 类断言必须逐场景实测**，尤其是「某事不会发生」（人工 commit 不会被记成 agent 的）。
  这次的三条高危分别藏在 rebase 重放、merge 的非编辑器路径、trailer 块的段落规则里，
  读代码看不出来。
- **模拟「人工操作」时要 `env -u CLAUDE_CODE_SESSION_ID`。** agent 的 shell 自带这个变量，
  不去掉则「人工提交」全是假的——我第一次验证 hook 就是这样把一轮实验全污染了。
- **语料是活的，所有语料级数字必须带测量日期。** 同一批量 09-08 测 402/277/90，09-09 测 405/282/92——我们工作时它一直在增长。
- **差值的归因必须单独验证，不能顺手挂在最近的改动上。** 那个 +2 曾被归因为「刚修的数组形态缺陷恢复了 2 条」，听起来合理但是错的：
  1145 个 `is_error` 块全是字符串形态，修复是纯防御，真因是语料增长。「刚修好的那个 bug」是最省力的解释，也正因为省力所以危险。
